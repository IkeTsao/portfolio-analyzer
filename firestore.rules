rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允許所有已認證用戶（包括匿名用戶）完全訪問
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

